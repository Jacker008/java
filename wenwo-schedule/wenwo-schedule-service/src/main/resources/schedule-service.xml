<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd  
             http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd  
             http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd">

	<context:annotation-config />

	<context:component-scan base-package="com.wenwo.schedule" />

	<context:property-placeholder location="classpath:schedule-service.properties" file-encoding="utf8" />

	<bean id="jobDispatcher" class="com.wenwo.schedule.JobDispatcher">
		<property name="master" value="${master.host}" />
		<property name="masterPort" value="${master.port}" />
		<property name="queueName" value="${job.queue-name}" />
		<property name="mqHost" value="${rabbitmq.host}" />
		<property name="mqPort" value="${rabbitmq.port}" />
		<property name="mqUsername" value="${rabbitmq.username}" />
		<property name="mqPassword" value="${rabbitmq.password}" />
	</bean>

	<bean class="com.google.gson.Gson" />
	
	<bean id="mongo" class="com.mongodb.MongoClient">
		<constructor-arg value="${mongo.host}"></constructor-arg>
		<constructor-arg value="${mongo.port}"></constructor-arg>
	</bean>
	
	<bean class="org.springframework.data.mongodb.core.MongoTemplate">
		<constructor-arg ref="mongo"></constructor-arg>
		<constructor-arg value="${mongo.database}"></constructor-arg>
	</bean>
	
	<bean class="com.wenwo.commons.util.mail.MailSmtpProfile">
		<constructor-arg value="${alert.mail.host}" />
		<constructor-arg value="${alert.mail.port}" />
		<constructor-arg value="${alert.mail.username}" />
		<constructor-arg value="${alert.mail.username}" />
		<constructor-arg value="${alert.mail.password}" />
		<constructor-arg value="${alert.mail.senderName}" />
		<constructor-arg value="${alert.mail.auth}" />
	</bean>
	
	<bean class="com.wenwo.schedule.slaver.UserEmail"/>

</beans>
